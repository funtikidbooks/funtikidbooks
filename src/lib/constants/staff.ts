// Quick-pick suggestions for chức danh (job title) — a free-text field, not
// a permission level, so this is just a datalist of common studio titles
// rather than a fixed enum.
export const JOB_TITLE_SUGGESTIONS = [
  "Art Director",
  "Team Leader",
  "Project Manager",
  "Art Lead",
  "2D Illustrator",
  "3D Artist",
  "Biên tập viên",
];

// Departments group members for the Thành viên directory's filter tabs —
// unlike job title, this is a fixed set so the tabs stay stable.
export const DEPARTMENTS = ["Studio Admin", "Illustrator", "3D / Design", "Marketing"];
